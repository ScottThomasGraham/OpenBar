# OS
.DS_Store
Thumbs.db

# Build artifacts
/build/
/out/
*.o
*.elf
*.map

# Upstream forks are cloned/worked on separately, not vendored here
/forks/

# Research scratch clones
/.research/

# scratch / intermediate render captures (leading underscore = throwaway)
_*.png
/docs/mockups/gallery/r-*.png

# pristine upstream reference checkouts (fetch with references/fetch-references.sh)
/upstream/
